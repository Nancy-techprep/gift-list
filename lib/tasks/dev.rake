unless Rails.env.production?
  #namespace :dev do
   # desc "TODO"
task({ :sample_data => :environment}) do
  
 # 5.times do |i|
  #  user = User.create(
   #   email: "#{name}"
    #  name: ""
     # password: ""

   # )

  # add 5 users
end
end
